service: fixture-service

provider:
  name: aws
  runtime: nodejs18.x

functions:
  getUser:
    handler: src/users.getUser
    events:
      - http:
          method: get
          path: /users/{id}
  createOrder:
    handler: src/orders.createOrder
    events:
      - http:
          method: post
          path: /orders
