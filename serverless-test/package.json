{
  "name": "tests-serverless",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "npx serverless dev",
    "deploy": "npx serverless deploy",
    "deploy:dev": "cd .. && npm run build && cd ./serverless-test && npx serverless deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless": "^4.4.15",
    "@drokt/serverless": "1.0.0",
    "@drokt/core": "1.0.0",
    "@drokt/openapi-plugin": "1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/serverless": "^3.12.25",
    "typescript": "^5.7.2"
  }
}
